{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "运行脚本: start",
            "request": "launch",
            "command": "pnpm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}